# Prisma Setup

This folder is scaffolded for Prisma.

- `schema.prisma` includes generator and datasource only.
- No product models are defined yet.
- Copy `.env.example` to `.env` and set `DATABASE_URL` when ready.

